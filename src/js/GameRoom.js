import React, {useState, useEffect} from 'react';
import "../styles/game_room.scss"

export default class GameRoom extends React.Component {

    constructor (props)
    {
        super(props);
        this.state = 
        {
        }
    }

    render () {
        return (
            <>
            </>
        )
    }
}