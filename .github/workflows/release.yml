name: Automated Releases

on:
  release:
    types: [created]

jobs:
  release-linux:

    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [12.x]

    steps:
    - uses: actions/checkout@v2
    - name: Use Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v1
      with:
        node-version: ${{ matrix.node-version }}
    - run: npm install
    - run: npm run bundle
    - run: npm run make-linux
    - uses: thedoctor0/zip-release@master
      with:
        filename: 'everyone.dance-linux-x64.zip'
        path: 'out/everyone.dance-linux-x64'
    - uses: shogo82148/actions-upload-release-asset@v1
      with:
          upload_url: ${{ github.event.release.upload_url }}
          asset_path: everyone.dance-linux-x64.zip

      
#   release-macos:

#     runs-on: macos-latest

#     strategy:
#       matrix:
#         node-version: [10.x, 12.x, 14.x, 15.x]

#     steps:
#     - uses: actions/checkout@v2
#     - name: Use Node.js ${{ matrix.node-version }}
#       uses: actions/setup-node@v1
#       with:
#         node-version: ${{ matrix.node-version }}
#     - run: npm run bundle
#     - run: npm run make
#     - run: npm test
      
#   release-windows:

#     runs-on: windows-latest

#     strategy:
#       matrix:
#         node-version: [10.x, 12.x, 14.x, 15.x]

#     steps:
#     - uses: actions/checkout@v2
#     - name: Use Node.js ${{ matrix.node-version }}
#       uses: actions/setup-node@v1
#       with:
#         node-version: ${{ matrix.node-version }}
#     - run: npm run bundle
#     - run: npm run make
#     - run: npm test
